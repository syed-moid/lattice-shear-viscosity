# Template only — copy to .env and paste your real values there (.env is gitignored).
# Load it only when running scripts — do NOT export these globally in .zshrc/.bashrc.
AZURE_SUBSCRIPTION_ID=REPLACE_ME
AZURE_TENANT_ID=REPLACE_ME
AZURE_GROUP=rg-lattice-shear-viscosity
LOCATION=eastus
AZURE_CLIENT_ID=REPLACE_ME
AZURE_CLIENT_SECRET=REPLACE_ME
