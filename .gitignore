# manuscript & review material — never public
manuscript/
**/[Rr]eferee*

# library-paper PDFs and figure crops are copyrighted — only the digitized
# derived data (CSVs + per-figure READMEs) are distributed, with citation
references/*
!references/digitized/
!references/digitized/**

# internal working logs — not part of the data-availability record
monitoring/
data/processed/reports/GATE_1p.md

# documents and rendered figure outputs — sources/scripts are committed,
# outputs regenerate; paper PDFs are copyrighted
*.pdf
*.docx
*.png
*.jpg

# secrets
.env
*.pem
*credentials*
*secrets*

# python
.venv/
venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# pseudopotentials — redistribution license not stated in the files;
# download sources + MD5s recorded in dft/qe/pseudopotentials/SOURCES.md
*.UPF

# heavy calculation outputs
data/raw/
*.wfc*
*.save/
fc2.hdf5
fc3.hdf5
kappa-*.hdf5
*.out.tar.gz

# OS / editors
.DS_Store
~$*
